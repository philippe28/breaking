import { FilterCharactersPipe } from './filter-characters.pipe';

describe('FilterCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
